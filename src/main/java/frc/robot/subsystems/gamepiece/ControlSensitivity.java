package frc.robot.subsystems.gamepiece;

/** Classifies whether a mechanism is primarily controlled by position or velocity. */
public enum ControlSensitivity {
  POSITION_SENSITIVE,
  VELOCITY_SENSITIVE
}
